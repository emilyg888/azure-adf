CREATE TABLE IF NOT EXISTS md_dataset (
    dataset_id STRING,
    dataset_name STRING,
    domain STRING,
    owner STRING,
    classification STRING,
    is_active BOOLEAN,
    created_at TIMESTAMP
);
