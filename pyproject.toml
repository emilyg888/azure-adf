[project]
name = "fabric-foundry"
version = "0.1.0"
description = "Metadata-driven Azure Fabric data ingestion and transformation framework MVP."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
